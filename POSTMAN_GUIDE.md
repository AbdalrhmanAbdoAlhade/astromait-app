# AstroMait Postman Collection

هذه المجموعة تغطي مسارات API الحالية للمشروع. استورد ملف `AstroMait.postman_collection.json` في Postman، ثم أنشئ بيئة من المتغيرات الموجودة داخل المجموعة أو استخدمها مباشرة.

## طريقة التشغيل

شغّل Laravel عبر `php artisan serve`، ثم ابدأ بطلب **تسجيل الدخول** أو **تسجيل مستخدم جديد**. بعد نجاح الطلب، تحفظ اختبارات Postman قيمة التوكن في متغير `token`، لذلك ستعمل الطلبات المحمية تلقائيًا عبر Bearer Token.

| المتغير | الوظيفة | القيمة الافتراضية |
|---|---|---|
| `base_url` | عنوان الخادم دون `/api/v1` | `http://127.0.0.1:8000` |
| `token` | Bearer Token المحفوظ بعد الدخول | فارغ |
| `product`, `service`, `auction` | معرفات الموارد للتجربة | `1` |
| `orderNumber` | رقم الطلب للتجربة | مثال تجريبي |
| `vendorProfile` | معرف التاجر للإدارة | `1` |
| `payoutRequest` | معرف طلب التحويل | `1` |
| `settlement` | معرف التسوية | `1` |

## ترتيب الاختبار المقترح

ابدأ بالمصادقة، ثم أنشئ أو استخدم بيانات الكتالوج، وأضف عنصرًا إلى السلة، ثم نفّذ Checkout. بالنسبة لمحفظة التاجر، يجب أن يكون لديك مستخدم بدور `vendor` وملف `vendor_profile`، ثم استخدم حساب التحويل قبل إنشاء طلب السحب. بالنسبة للإدارة، استخدم مستخدمًا بدور `admin` لاعتماد التسويات وطلبات التحويل.

## شرح المجموعات

| المجموعة | الشرح |
|---|---|
| المصادقة | التسجيل والدخول وقراءة المستخدم الحالي وتسجيل الخروج. |
| الكتالوج العام | قراءة المنتجات والخدمات والتصنيفات والبنرات والمقالات والمزادات والشهادات. |
| المستخدم والطلبات | السلة، الكوبونات، العناوين، Checkout، الطلبات، الحجوزات والمزايدات. |
| البائع | إدارة منتجاته وخدماته ومزاداتِه وكوبوناته وطلبات الشهادات. |
| المحفظة والتسويات | الأرصدة، سجل القيود، حسابات التحويل، طلبات السحب والتسويات. |
| الإدارة | اعتماد التجار، إدارة التحويلات والتسويات، وWebhook الدفع. |

## حالات مهمة

طلب التحويل ينقل المبلغ من `available_balance` إلى `held_balance`. رفضه يعيد المبلغ إلى الرصيد المتاح، وتسجيله كمدفوع ينقله إلى `paid_balance`. لا تستخدم بيانات IBAN الحقيقية في بيئة الاختبار، ويجب التحقق من توقيع Webhook بوابة الدفع قبل الإنتاج.

## ملاحظات

بعض أمثلة JSON تعتمد على أسماء الحقول الموجودة في المشروع وقد تحتاج حقولًا إضافية إذا أضافت قواعد التحقق في الكنترولر. يمكن تعديل body مباشرة من Postman دون تغيير بنية المجموعة.
